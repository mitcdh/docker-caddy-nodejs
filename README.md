# Docker Caddy Server + nodejs

Installs nodejs with grunt-cli and gulp-cli packages globally installed

### Usage
````bash
docker pull mitcdh/caddy-nodejs
````

### Structure
* `/www`: Web root

### Exposed Ports
* `2015`: http web server